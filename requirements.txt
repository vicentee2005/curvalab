# Dependencias del backend desplegado (las instala el Dockerfile).
#
# Sin pandas: el importador de CSV/Excel se reescribió con openpyxl y el módulo
# csv de la biblioteca estándar, así que ya no hace falta.
# Sin pytest: los tests se ejecutan en local, no dentro de la imagen.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic>=2.0
python-multipart==0.0.20
numpy==2.2.6
scipy==1.16.3
sympy==1.14.0
openpyxl>=3.1
